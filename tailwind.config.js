/** Tailwind CSS Config (ESM) duplicated from tailwind.config.cjs so Angular builder auto-detects */
/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './src/**/*.{html,ts}'
    ],
    theme: { extend: {} },
    plugins: []
};
